# Judge0 Configuration

# Redis Configuration
REDIS_HOST=judge0-cache
REDIS_PORT=6379
REDIS_PASSWORD=judge0_redis_secret

# PostgreSQL Configuration
POSTGRES_HOST=judge0-db
POSTGRES_PORT=5432
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0_db_secret

# Default limitations for code execution
ENABLE_NETWORK=false
CPU_TIME_LIMIT=10
MEMORY_LIMIT=128000
MAX_PROCESSES_AND_OR_THREADS=60
MAX_FILE_SIZE=4096
